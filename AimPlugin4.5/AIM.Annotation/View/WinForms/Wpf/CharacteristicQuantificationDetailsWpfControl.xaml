<UserControl x:Class="AIM.Annotation.View.WinForms.Wpf.CharacteristicQuantificationDetailsWpfControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Margin="0">
    <UserControl.Resources>
        <Style TargetType="ToolTip">
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding}" MaxWidth="250" TextWrapping='Wrap' />
                        </StackPanel>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
        <StackPanel Margin="10,0,0,0" Name="_stackPanel">
        <Grid Name="_grid">
            <Label HorizontalAlignment="Left" Margin="0,6" Name="_lblName" VerticalAlignment="Top" Padding="0">
                <TextBlock TextWrapping="Wrap" Name="_charQuantifiactionName">Name:</TextBlock>
            </Label>
        </Grid>
        </StackPanel> 
</UserControl>
